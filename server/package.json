{
  "name": "server",
  "version": "1.0.0",
  "description": "first nodejs server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "foldir society",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
